/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  allowedDevOrigins: ['1ggb09v5-5000.inc1.devtunnels.ms', 'recolor-partly-prozac.ngrok-free.dev'],
};

export default nextConfig;
